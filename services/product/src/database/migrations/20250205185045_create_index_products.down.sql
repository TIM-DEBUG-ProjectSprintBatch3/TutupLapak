-- Drop index untuk sku
DROP INDEX IF EXISTS idx_sku_hash;

-- Drop index untuk product_id
DROP INDEX IF EXISTS idx_product_id_hash;

-- Drop index untuk category
DROP INDEX IF EXISTS idx_category_hash;
